extends layout

block content
  h1 All User Accounts
  
  each user in users 
    h3 User Name: #{user.userName}, Email: #{user.email}
    button(onclick="showOrderHistory('"+user.userName+"')") Show Order history
    hr
    br
    br

  script(src='../javascripts/users.js')

